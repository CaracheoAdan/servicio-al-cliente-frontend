# Dev: Vite proxies /api → localhost:5041 (see vite.config.ts)
VITE_API_URL=/api/v1

# Production (IIS): use same-host so the browser calls hostname:5041
# VITE_API_URL=same-host
# VITE_API_PORT=5041

# Or absolute URL (portal-style Option A):
# VITE_API_URL=http://192.168.88.69:5041/api/v1
